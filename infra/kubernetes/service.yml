apiVersion: v1
kind: Service
metadata:
  name: habitforge-service
  namespace: habitforge
  labels:
    app: habitforge
    component: api
spec:
  type: ClusterIP
  selector:
    app: habitforge
    component: api
  ports:
    - name: http
      port: 80
      targetPort: 3002
      protocol: TCP
